\begin{Schunk}
\begin{Sinput}
 plot(gradrate ~ private, data=dat, xlab="Grip usnength", ylab="Supervisor Rating")
 abline(mod1)
 newdf <- data.frame(private=levels(dat$private))
 pconf <- predict(mod1, interval="confidence", newdata=newdf)